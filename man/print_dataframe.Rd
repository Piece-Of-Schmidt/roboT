% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_text_functions.R
\name{print_dataframe}
\alias{print_dataframe}
\title{Pretty-print a data frame in the console}
\usage{
print_dataframe(
  df,
  col_prop = NULL,
  buffer = 2,
  sep = " | ",
  line_char = "-",
  del_special_chars = "\\r"
)
}
\arguments{
\item{df}{A data frame to be printed.}

\item{col_prop}{A numeric vector of relative column widths (values between 0 and 1).
If \code{NULL}, columns are equally spaced.}

\item{buffer}{Integer to reduce each column width slightly (default: 2).}

\item{sep}{Character string used as column separator (default: \code{" | "}).}

\item{line_char}{Character used to draw row separators (default: \code{"-"}).}

\item{del_special_chars}{Character pattern to remove from cells (e.g. \code{"\\r"}). Set to \code{NULL} to skip.}
}
\value{
No return value. Output is printed to the console.
}
\description{
Formats and prints a data frame to the R console with controlled column widths and wrapping.
Useful for console-based output where visual clarity is important.
}
\examples{
df <- data.frame(a = c("short", "a very long piece of text"), b = 1:2)
print_dataframe(df)
}
