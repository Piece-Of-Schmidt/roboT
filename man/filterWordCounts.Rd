% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_corpus_cleaning.R
\name{filterWordCounts}
\alias{filterWordCounts}
\title{Filter texts based on word count thresholds}
\usage{
filterWordCounts(corpus, lower_thresh = 0, upper_thresh = 1)
}
\arguments{
\item{corpus}{A \code{textmeta} object with tokenized texts (as lists).}

\item{lower_thresh}{Lower threshold. Either a number of tokens or a quantile (e.g. \code{0.05}).}

\item{upper_thresh}{Upper threshold. Either a number of tokens or a quantile (e.g. \code{0.95}).}
}
\value{
A filtered \code{textmeta} object containing only texts within the given range.
}
\description{
Extends \code{tosca::filterCounts()} by allowing both absolute and relative
thresholds (quantiles) for word counts.
}
\examples{
# Keep only the middle 90\% of texts by word count
# corpus <- filterWordCounts(corpus, lower_thresh = 0.05, upper_thresh = 0.95)
}
