% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/07_lda_evaluation.R
\name{multipleLDAs}
\alias{multipleLDAs}
\title{Run multiple LDA models with varying parameters}
\usage{
multipleLDAs(
  docs,
  vocab,
  ...,
  func = "LDAgen",
  runs = "all",
  seed = 1337,
  savelogs = T
)
}
\arguments{
\item{docs}{Either a single document list or a list of such objects (as in \code{LDAprep()}).}

\item{vocab}{Either a single vocabulary or a list of vocabularies.}

\item{...}{Named arguments to be passed to \code{LDAgen()} (e.g., \code{K}, \code{alpha}, \code{beta}).}

\item{func}{Function name to use for LDA generation (default: \code{"LDAgen"}).}

\item{runs}{Either \code{"all"} or an integer to subsample from all grid combinations.}

\item{seed}{Random seed for reproducibility (default: 1337).}

\item{savelogs}{Logical. Save parameter combinations to \code{"model_logs.csv"}?}
}
\value{
A list of LDA result objects, named \code{run1}, \code{run2}, ...
}
\description{
Automates repeated LDA estimation across a grid of parameter combinations and/or corpora.
Optionally saves parameter log to disk.
}
\examples{
# multipleLDAs(docs, vocab, K = c(10, 15), alpha = 0.1, runs = 2)
}
