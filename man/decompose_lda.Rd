% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_lda_calculation.R
\name{decompose_lda}
\alias{decompose_lda}
\title{Aggregate LDA topic scores by metadata group and time}
\usage{
decompose_lda(
  document_topic_matrix,
  lookup_dict,
  select = 1:nrow(document_topic_matrix),
  unit = "month",
  tnames = paste0("topic", select),
  plot = F,
  out = "melt"
)
}
\arguments{
\item{document_topic_matrix}{A matrix of topic scores (topics × documents).}

\item{lookup_dict}{A data frame with three columns: \code{id}, \code{date}, and \code{group}.}

\item{select}{Vector of topic indices to include (default: all).}

\item{unit}{Time unit for aggregation, passed to \code{lubridate::floor_date()} (e.g., \code{"month"}).}

\item{tnames}{Optional names for selected topics.}

\item{plot}{Logical. If \code{TRUE}, plots the result using \code{ggplot2}.}

\item{out}{Format of output: \code{"melt"} (long format) or \code{"wide"} (default: \code{"melt"}).}
}
\value{
A data frame of aggregated topic counts by date, group, and topic.
}
\description{
Decomposes a document-topic matrix by grouping documents by a meta variable
(e.g., source or region) and aggregating over time (e.g., monthly).
}
\examples{
# decompose_lda(theta, lookup_dict, unit = "month", plot = TRUE)
}
