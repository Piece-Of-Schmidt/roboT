% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05_corpus_cleaning_auto.R
\name{clean_complete}
\alias{clean_complete}
\title{Perform full preprocessing pipeline on a textmeta corpus}
\usage{
clean_complete(
  corpus,
  shorten_meta = F,
  remove_na_dates = F,
  remove_na_sources = F,
  remove_na_titles = F,
  sort_meta = T,
  min_text_length = 0,
  max_text_length = 1,
  check_dups_id = T,
  check_dups_duplist = F,
  check_dups_textleads = 120,
  check_dups_titles = T,
  check_dups_textleads_unit = "week",
  check_dups_titles_unit = "day",
  check_dups_ignore_case = T,
  clean_titles = T,
  utf8 = F,
  calculate_text_len_type = "bytes",
  clean_memory = F,
  hbweltsz = F,
  verbose = F
)
}
\arguments{
\item{corpus}{A \code{textmeta} object (see \pkg{tosca}).}

\item{shorten_meta}{Logical. Keep only essential columns in \code{meta} (id, date, title, resource)?}

\item{remove_na_dates}{Logical. Remove texts without date information?}

\item{remove_na_sources}{Logical. Remove texts without resource information?}

\item{remove_na_titles}{Logical. Remove texts without title information?}

\item{sort_meta}{Logical. Should meta data be chronologically sorted? (default: \code{TRUE})}

\item{min_text_length}{Minimum number of characters/tokens (absolute or quantile) to keep a text.}

\item{max_text_length}{Maximum number of characters/tokens (absolute or quantile) to keep a text.}

\item{check_dups_id}{Logical. Remove duplicated text IDs?}

\item{check_dups_duplist}{Logical. Use \code{tosca::duplist()} to remove near-duplicate texts?}

\item{check_dups_textleads}{Integer. Number of leading characters to check for duplicates (or \code{NULL} to skip).}

\item{check_dups_titles}{Logical. Remove duplicates based on identical titles?}

\item{check_dups_textleads_unit}{Time unit for \code{filterDups_leads()} (default: \code{"week"}).}

\item{check_dups_titles_unit}{Time unit for \code{filterDups_titles()} (default: \code{"day"}).}

\item{check_dups_ignore_case}{Logical. Should duplicate detection ignore case?}

\item{clean_titles}{Logical. Clean up multiple whitespaces and HTML escape characters in titles?}

\item{utf8}{Logical. Convert text and title encoding to UTF-8?}

\item{calculate_text_len_type}{Type for \code{nchar()} calculation (\code{"bytes"} or \code{"chars"}).}

\item{clean_memory}{Logical. Call \code{gc()} after each major operation? Useful for large corpora.}

\item{hbweltsz}{Logical. Reassign sources according to common DoCMA corpus consisting of HB/Welt/SZ corpora.}

\item{verbose}{Logical. Print detailed info on removed duplicates.}
}
\value{
A cleaned \code{textmeta} object, reduced to documents meeting all constraints.
}
\description{
Applies a comprehensive cleaning routine to a \code{textmeta} object. Includes filtering
by text length, removal of duplicates (by ID, title, text lead), missing values,
UTF-8 conversion, resource redefinition (for HB/Welt/SZ), and optional memory cleanup.
}
\examples{
# Clean corpus using default settings
# corpus_clean <- clean_complete(corpus, min_text_length = 750, max_text_length = 0.95)
}
