% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_corpus_preprocessing.R
\name{plotSources}
\alias{plotSources}
\title{Plot source frequencies in a textmeta corpus}
\usage{
plotSources(
  corpus,
  category = "resource",
  unit = "month",
  area_alpha = 0.3,
  area_position = "identity",
  ...,
  smooth = T,
  span = 0.1,
  se = T
)
}
\arguments{
\item{corpus}{A \code{textmeta} object as used in the \pkg{tosca} package.}

\item{category}{Character string specifying the metadata column to group by (default: \code{"resource"}).}

\item{unit}{Time unit for aggregation (e.g., \code{"month"}, \code{"week"}, \code{"day"}). Passed to \code{lubridate::floor_date()}.}

\item{area_alpha}{Transparency for \code{geom_area()} (default: \code{0.3}).}

\item{area_position}{Position adjustment for \code{geom_area()} (e.g., \code{"stack"}, \code{"identity"}; default: \code{"identity"}).}

\item{...}{Additional arguments passed to \code{geom_area()}.}

\item{smooth}{Logical. Should a smoothed trend line be added? (default: \code{TRUE})}

\item{span}{Smoothing span for \code{geom_smooth()} (default: \code{0.1}).}

\item{se}{Logical. Should confidence intervals be shown in smoothing? (default: \code{TRUE})}
}
\value{
Invisibly returns the aggregated data frame with columns \code{date}, \code{category_to_group_after}, and \code{n}.
A \code{ggplot2} plot is printed to the console.
}
\description{
Aggregates and visualizes the number of documents over time,
grouped by a specified metadata column (e.g., "resource" or "medium").
Optionally includes smoothing lines using \code{geom_smooth()}.
}
\examples{
# plotSources(corpus, category = "medium", unit = "month")
}
