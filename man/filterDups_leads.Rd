% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_corpus_cleaning.R
\name{filterDups_leads}
\alias{filterDups_leads}
\title{Remove duplicate articles based on text beginnings}
\usage{
filterDups_leads(
  corpus,
  checkFirstChars = 120,
  unit = "day",
  ignore.case = F,
  message = T,
  verbose = F
)
}
\arguments{
\item{corpus}{A \code{textmeta} object.}

\item{checkFirstChars}{Number of leading characters to use for duplication detection (default: \code{120}).}

\item{unit}{Time unit to limit comparisons (e.g., \code{"day"}, \code{"month"}, \code{"all"}).}

\item{ignore.case}{Logical. Should case be ignored in the comparison?}

\item{message}{Logical. Should summary info be printed? (default: \code{TRUE})}

\item{verbose}{Logical. Should the most frequent leads be printed? (default: \code{FALSE})}
}
\value{
A filtered \code{textmeta} object with duplicate leads removed.
}
\description{
Detects and removes duplicates based on the first characters of a text
(e.g., first 120 characters), optionally within time units.
}
\examples{
# corpus <- filterDups_leads(corpus, checkFirstChars = 100, unit = "day")
}
